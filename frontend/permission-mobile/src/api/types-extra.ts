// Kept as a small compatibility module for feature code that imports shared
// protocol types from a dedicated file.
export type { ApiErrorKind, ApiError, ApiResult, ApiResponse, PageQuery, PagedResult } from './types'

